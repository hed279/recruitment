1. After make, there will be two executable files: abserver and abclient. Run abserver on server machine and ablient on client machine.

2. Run abserver with no parameter. Then server will start.

3. Run abclient with server's IP. Get into CLI. Do operations.

4. File will be saved on server machine. Each time server starts, it will load content of the file.

